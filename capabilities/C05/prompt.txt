Capability qualification C05: Immutable reviewer handoff.

Exercise a fresh reviewer handoff using an explicit review bundle path and recorded hashes.

Record only the minimal structural observations required to evaluate the assertions in expected.json. Do not expose secrets, usernames, home directories, session IDs, private repository URLs, or full conversation transcripts.
